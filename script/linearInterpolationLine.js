/** @type {HTMLCanvasElement} */


const linearInterpolation = (u) => {
    if (u < 0 || u > 1) {
        return console.error(`LinearInterpolation(): u is only defined for 0 <= u <= 1 and ${u} is not within the span`);
    }

    ctx.moveTo
}
